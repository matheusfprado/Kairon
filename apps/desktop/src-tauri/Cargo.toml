[package]
name = "kairon"
version = "0.1.0"
description = "Kairon desktop assistant"
authors = ["Kairon"]
edition = "2021"

[lib]
name = "kairon_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
